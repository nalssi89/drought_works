# 권역별 누적강수 대시보드 디자인 계약

## 0. 기준 화면

- 사용자 제공 기상청 누적강수량 표 화면을 시각 계약으로 사용한다.
- 업무용 조회 화면답게 장식보다 빠른 날짜·기간 변경과 수치 비교를 우선한다.
- 원본의 남색 다단 표 머리글, 흰 바탕, 얇은 회색 격자, 조밀한 제어부를 유지한다.

## 1. 토큰

- `--navy-900: #173766` — 제목과 핵심 표 머리글
- `--navy-800: #23477f` — 선택 버튼과 보조 머리글
- `--blue-600: #2e62b7` — 링크와 포커스
- `--surface: #ffffff`
- `--surface-subtle: #f4f6f8`
- `--surface-info: #eef5ff`
- `--ink: #172033`
- `--muted: #5d6878`
- `--line: #cbd2dc`
- `--status-good: #168154`
- 임계값: 65% `#dbeafe`, 55% `#fff4b8`, 45% `#ffd6a3`
- 간격은 4px 배수, 제어 높이는 40px 이상, 본문은 14~16px를 사용한다.
- `--region-column: 100px` — 강원 3분할을 제외한 모든 권역 데이터 열의 공통 폭

## 2. 타이포그래피

- 한국어는 `Pretendard Variable`, `Noto Sans KR`, `Malgun Gothic`, 시스템 글꼴 순서다.
- 표 수치는 tabular-nums를 사용한다.
- 제목 32px/800, 섹션 제목 19px/700, 표 본문 14px/500을 기본으로 한다.

## 3. 레이아웃

- 상단: 서비스 제목·공식자료 상태·기준일.
- 제어부: 날짜 이동과 1·3·6·12개월·올해 누적·임의기간 선택을 같은 행군에 배치한다.
- 본문: 12개 권역 공식 표, 중부·남부·제주·전국 요약, 66개 지점 상세 순서다.
- 표는 데스크톱에서 한 화면 비교가 가능해야 하며 모바일에서는 가로 스크롤을 허용한다.
- 평년비 범례와 두 집계표의 전국 열 오른쪽 끝은 본문 컨테이너의 동일한 수직 경계에 맞춘다.

## 4. 구성요소

- `ControlBar`: 날짜 입력, 빠른 이동, 기간 선택, 최대 1년의 임의 시작·종료일, 당일 시간강수 반영 체크와 관측시각 입력.
- `RegionTable`: 강수량·평년값·평년비·평년대비 차이·최저순위.
- `AdminTable`: 중부·남부·제주·전국 집계.
- `StationTable`: 66개 지점의 공식 누적값과 평년비를 지점번호 오름차순으로 표시한다.
- `Freshness`: 공식 기준일, 조회 시각, 자료 제공 상태.

## 5. 상태와 접근성

- 선택된 기간은 배경과 글자색을 동시에 바꾼다.
- 모든 링크·버튼·입력은 키보드 포커스 고리를 표시한다.
- 색만으로 평년비를 구분하지 않고 수치를 함께 표기한다.
- 로딩·오류·자료 없음 상태를 문장으로 제공한다.
- 날짜·기간·시간 조건 변경은 별도 조회 버튼 없이 즉시 반영하고 갱신 중 상태를 알린다.
- 당일 시간강수 반영은 공식 일자료와 구분되는 추정 상태로 표시하며, 강수량만 선택 시각의 일누적으로 대체하고 평년값은 종료일의 일평년값 전체를 적용한다고 고지한다.

## 6. 반응형

- 960px 이하에서는 제어부를 줄바꿈한다.
- 섹션 제목이 줄바꿈되어도 평년비 범례는 표의 보이는 오른쪽 경계에 맞춘다.
- 720px 이하에서는 페이지 여백을 12px로 줄이고 표를 가로 스크롤한다.
- 표의 첫 열은 스크롤 중에도 보이도록 고정한다.

## 7. 모션

- 선택·호버의 색 변화만 120ms로 적용한다.
- 레이아웃 속성은 애니메이션하지 않는다.
- `prefers-reduced-motion`에서는 전환을 제거한다.
- 조건 변경 중에는 입력을 막지 않고 상태 문구만 갱신한다.

## 8. 수용한 제약

- 공식 화면의 높은 정보 밀도를 유지하므로 모바일에서는 가로 스크롤이 필요하다.
- 지도나 장식 이미지는 정확한 표 조회에 기여하지 않아 포함하지 않는다.
- 시간단위 평년값은 제공되지 않으므로 당일 평년값은 시각과 관계없이 해당 날짜의 일평년값 전체를 사용한다. 이 모드의 순위는 산출하지 않는다.
